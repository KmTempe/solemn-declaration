# Production Environment Configuration Template
# Copy this to .env.production and customize the paths for your system

# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=0
FLASK_SECRET=CHANGE_THIS_TO_A_RANDOM_64_CHAR_STRING

# SMTP Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
RECIPIENT_EMAIL=your-email@gmail.com

# Database Configuration
REDIS_URL=redis://redis:6379/0
MONGO_URL=mongodb://admin:${MONGO_ROOT_PASSWORD}@mongodb:27017/solemn_declarations?authSource=admin

# MongoDB Authentication
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=YourSecurePassword123!

# Data Storage Paths (Customize these for your system)
# For Windows, use forward slashes or double backslashes
# Examples:
#   Windows: C:/Docker/level7-form-data or C:\\Docker\\level7-form-data
#   Linux: /var/lib/level7-form-data
#   macOS: /Users/username/docker-data/level7-form

DATA_ROOT_PATH=I:/forma/solemn-declaration/production-data
REDIS_DATA_PATH=${DATA_ROOT_PATH}/RedisData
MONGODB_DATA_PATH=${DATA_ROOT_PATH}/MongoDB
APP_LOGS_PATH=${DATA_ROOT_PATH}/logs
NGINX_CONFIG_PATH=${DATA_ROOT_PATH}/nginx
NGINX_SSL_PATH=${DATA_ROOT_PATH}/ssl

# Security Settings (Production)
SESSION_COOKIE_SECURE=false  # Set to true if using HTTPS
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# Application Settings
FLASK_APP=app.py
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Optional: Custom domain
# DOMAIN=forms.yourdomain.com

# Optional: Additional security headers
# FORCE_HTTPS=true
# HSTS_MAX_AGE=31536000

# =============================================================================
# CONFIGURATION INSTRUCTIONS:
# =============================================================================
# 1. Copy this file to .env.production
# 2. Update DATA_ROOT_PATH to your desired location
# 3. Run: .\setup-production-dirs.ps1 -DataRootPath "YOUR_PATH"
# 4. Update SMTP credentials with your email settings
# 5. Change MONGO_ROOT_PASSWORD to a secure password
# 6. Update FLASK_SECRET with a new random key
# 7. Deploy with: .\deploy-prod.sh deploy
